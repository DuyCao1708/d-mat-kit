import { DNoticationConfig } from './notification-options';

/** Default notification configuration */
export const DEFAULT_D_NOTIFICATION_CONFIG: DNoticationConfig = {
  showClose: true,
  toastTimeout: 5,
  swipeableToast: true
};
